# Walk-LCP — Python dependencies for the standalone version (walk_lcp.py)
#
# Versions below are those used to produce the results in the manuscript.
# Adjust freely; the API surface used here is stable across recent minor
# versions. If you bump a major version, re-run test_walk_lcp.py to
# confirm the numerical results have not changed.

numpy==1.26.4
gdal==3.8.4
geopandas==0.14.3
shapely==2.0.3
fiona==1.9.6
pyproj==3.6.1

# Demo notebook (demo_test_run.ipynb)
matplotlib==3.8.3
jupyter==1.0.0

# Development / verification
pytest==8.1.1
